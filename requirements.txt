# Telegram bot (webhook mode)
python-telegram-bot[webhooks]==22.3

# Generic video/audio download (TikTok, Instagram, etc.)
yt-dlp>=2025.7.21

# API server for large-file download links
fastapi==0.116.1
uvicorn==0.35.0

# Async I/O (VDA/ytstream HTTP, Telegram file send, VDA file write)
aiohttp>=3.9.0
aiofiles==24.1.0

# System dependency (not installable via pip): FFmpeg must be on PATH
